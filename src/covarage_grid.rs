use super::cell::Cell;

struct CovarageGrid {
    cells: Vec<Cell>,
    size: usize,
}
impl CovarageGrid {
    // fn new(size: usize, limit: usize) -> Self {
    //     Self {
    //         cells: Vec::new(),
    //         size,
    //     }
    // }
}